<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhangchuangla.system.mapper.SysDictionaryMapper">

    <resultMap id="BaseResultMap" type="cn.zhangchuangla.system.model.entity.SysDictionary">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="status" column="status"/>
        <result property="description" column="description"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <select id="getDictionaryList" resultMap="BaseResultMap">
        select *
        from sys_dictionary
        <where>
            <if test="request.name != null and  request.name != ''">
                and `name` like concat('%',#{request.name},'%')
            </if>
            <if test="request.description != null and request.description != ''">
                and `description` like concat('%',#{request.description},'%')
            </if>
            <if test="request.status != null">
                and `status` = #{request.status}
            </if>
        </where>
    </select>
    <select id="getDictionaryCountExcludeCurrentId" resultType="java.lang.Long">
        select count(*)
        from sys_dictionary
        where name = #{dictName}
          and id != #{id}
    </select>

</mapper>
