<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhangchuangla.system.mapper.SysDictMapper">

    <resultMap id="BaseResultMap" type="cn.zhangchuangla.system.model.entity.SysDict">
        <id property="id" column="id"/>
        <result property="dictCode" column="dict_code"/>
        <result property="name" column="name"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="isDeleted" column="is_deleted"/>
    </resultMap>
    <select id="listDict" resultMap="BaseResultMap">
        select id, dict_code, name, status, remark, create_time, create_by, update_time, update_by, is_deleted
        from sys_dict
        <where>
            <if test="request != null">
                <if test="request.id != null">
                    and id = #{request.id}
                </if>
                <if test="request.dictCode != null and request.dictCode != ''">
                    and dict_code like concat('%', #{request.dictCode}, '%')
                </if>
                <if test="request.name != null and request.name != ''">
                    and name like concat('%', #{request.name}, '%')
                </if>
                <if test="request.status != null">
                    and status = #{request.status}
                </if>
            </if>
            and is_deleted = 0
        </where>
        order by create_time desc
    </select>

</mapper>
