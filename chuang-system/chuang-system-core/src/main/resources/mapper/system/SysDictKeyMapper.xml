<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhangchuangla.system.mapper.SysDictKeyMapper">

    <resultMap id="BaseResultMap" type="cn.zhangchuangla.system.model.entity.SysDictKey">
        <id property="id" column="id"/>
        <result property="dictKey" column="dict_key"/>
        <result property="dictName" column="dict_name"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="listDictKey" resultMap="BaseResultMap">
        SELECT
        id,
        dict_key,
        dict_name,
        status,
        create_time,
        update_time,
        create_by,
        update_by,
        remark
        FROM sys_dict_key
        <where>
            <if test="request != null">
                <if test="request.dictKey != null and request.dictKey != ''">
                    AND dict_key LIKE CONCAT('%', #{request.dictKey}, '%')
                </if>
                <if test="request.dictName != null and request.dictName != ''">
                    AND dict_name LIKE CONCAT('%', #{request.dictName}, '%')
                </if>
                <if test="request.status != null">
                    AND `status` = #{request.status}
                </if>
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
