<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhangchuangla.system.mapper.SysRoleMenuMapper">

    <resultMap id="RoleMenuResultMap" type="cn.zhangchuangla.system.model.entity.SysRoleMenu">
        <result property="roleId" column="role_id" jdbcType="BIGINT"/>
        <result property="menuId" column="menu_id" jdbcType="BIGINT"/>
    </resultMap>

    <select id="countRoleMenuByMenuId" resultType="Integer" parameterType="Long">
        select count(1)
        from sys_role_menu
        where menu_id = #{menuId}
    </select>

    <select id="checkMenuExistRole" resultType="java.lang.Integer" parameterType="java.lang.Long">
        select count(1)
        from sys_role_menu
        where menu_id = #{menuId}
    </select>

    <select id="selectMenuListByRoleId" resultType="java.lang.Long">
        select menu_id
        from sys_role_menu
        where role_id = #{roleId}
    </select>


    <delete id="deleteRoleMenuByRoleId" parameterType="Long">
        delete
        from sys_role_menu
        where role_id = #{roleId}
    </delete>

    <insert id="batchInsertRoleMenu">
        insert into sys_role_menu(role_id, menu_id) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.roleId},#{item.menuId})
        </foreach>
    </insert>
</mapper>
